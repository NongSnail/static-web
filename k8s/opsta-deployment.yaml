apiVersion: apps/v1
kind: Deployment
metadata:
  name: opsta-dev-static-web
  namespace: student41-opsta-dev
  labels:
    app: opsta-dev-static-web
spec:
  replicas: 1
  selector:
    matchLabels:
      app: opsta-dev-static-web
  template:
    metadata:
      labels:
        app: opsta-dev-static-web
    spec:
      containers:
      - name: opsta-dev-static-web
        image: ghcr.io/nongsnail/static-web:dev
        imagePullPolicy: Always
        ports:
        - containerPort: 80
          name: web-port
          protocol: TCP
        livenessProbe:
          httpGet:
            path: /
            port: 80
            scheme: HTTP
        readinessProbe:
          httpGet:
            path: /
            port: 80
            scheme: HTTP
      imagePullSecrets:
      - name: registry-github